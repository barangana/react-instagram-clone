import { useContext } from "react";
import FirebaseContext from "../context/firebase";

function Header() {
  return (
    <div>
      <p>I am the header</p>
    </div>
  );
}

export default Header;
